# Persistent Database (SQLite)

Replace the in-memory activity and participant storage with a persistent SQLite database. Store users, activities, committees, events, and feedback for data durability and reliability.